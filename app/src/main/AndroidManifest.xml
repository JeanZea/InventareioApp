<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.isil.inventario">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MenuActivity" />
        <activity
            android:name=".activities.company.activities.EmpresaActivity"
            android:label="@string/title_activity_empresa"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.company.activities.EditEmpresa" />
        <activity android:name=".activities.company.activities.NuevaEmpresa" />
        <activity android:name=".activities.company.activities.EmpresaDetails" />
        <activity android:name=".activities.products.activities.ProductsActivity" />
        <activity
            android:name=".activities.products.activities.ListProductActivity"
            android:label="@string/title_activity_list_product"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.products.activities.AddProductActivity" />
        <activity android:name=".activities.category.activities.CategoryActivity"></activity>
    </application>

</manifest>